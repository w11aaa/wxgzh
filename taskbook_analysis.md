# 任务书分析报告（MVP导向）

## 1. 总体评价

该任务书结构完整、目标明确，采用了典型的 **“先可上线、再逐步增强”** 的 MVP 路线，阶段划分（需求 → 骨架 → 核心能力 → 微信闭环 → 上线）合理，适合单人或小团队推进。

综合评估：

- **可执行性：高**（任务拆解到模块级）
- **上线可达成性：中高**（依赖微信能力确认与采集稳定性）
- **进度风险：中**（阶段三、四工作量明显偏大）

---

## 2. 优点（做得好的部分）

1. **范围控制清晰**：明确将推荐算法、本地大模型、多租户等排除在首期之外，有利于按期上线。
2. **技术栈一致性高**：FastAPI + PostgreSQL + Redis + Celery + Docker Compose 是成熟组合，学习成本与维护成本可控。
3. **闭环意识强**：不仅有采集和处理，也覆盖了日报生成、推送与日志追踪，能够形成真实用户价值。
4. **风险前置**：任务书已识别抓取不稳定、微信能力限制、周期失控等关键风险，方向正确。

---

## 3. 主要风险与缺口（建议重点补充）

### 3.1 里程碑缺少“可量化验收口径”

当前多为“可运行/可实现”描述，建议每阶段增加 3～5 个可量化指标，例如：

- 采集成功率（24h）≥ 95%
- 日报生成耗时 ≤ 3 分钟
- 微信推送成功率 ≥ 98%
- 失败任务重试后恢复率 ≥ 90%

否则上线前很难判断“是否达标”。

### 3.2 阶段三（核心功能）体量偏大

阶段三同时包含来源管理、采集器、解析、去重、分类、摘要、日报、调度，耦合度高。
建议再拆成 **3A（采集入库闭环）/3B（去重分类）/3C（日报生成）** 三个子里程碑，降低延期风险。

### 3.3 微信推送路径需要提前“能力兜底”

任务书提到“订阅通知或客服消息”，但不同公众号类型、认证状态、行业模板限制较多。
建议在阶段一即输出“能力矩阵”（可用/不可用/替代方案），并准备短信、邮件或站内页链接作为兜底通道（至少在内部测试期可用）。

### 3.4 缺少“合规与版权策略”明确条款

既然涉及内容采集，建议单列：

- 仅采集公开可访问且允许引用的来源
- 保留来源链接与署名
- 提供内容下线机制
- 控制正文转载比例（优先摘要+跳转原文）

### 3.5 运维部分建议增加“最低监控集”

上线可用不等于可运维。建议定义最小监控面板：

- API 可用性与延时
- Celery 队列积压长度
- 采集任务成功率/失败率
- 推送成功率与错误码分布
- PostgreSQL 连接池占用

---

## 4. 工期评估与现实排期建议

原计划总计 **20～29 天**，在“单人开发 + 联调 + 部署 + 调优”场景下偏乐观。

更稳妥的预期：

- **MVP 可内测：4～6 周**（与任务书末尾建议一致）
- **可小规模正式运行：6～8 周**（含 1～2 周稳定性修复）

建议采用“2 周一个迭代”的节奏：

- 迭代1：项目骨架 + 数据库 + 调度框架 + 最小采集链路
- 迭代2：去重分类 + 日报生成 + 基础后台接口
- 迭代3：微信接入 + 订阅管理 + 推送日志
- 迭代4：稳定性、告警、部署与验收

---

## 5. MVP优先级重排（强烈建议）

将功能分为 P0/P1/P2：

### P0（必须有，决定能否上线）

- 来源管理（基础 CRUD）
- 至少一种稳定采集方式（优先 RSS）
- 文章入库 + URL 去重
- 每日日报生成（Markdown 即可）
- 公众号关注建档 + 基础推送
- 抓取日志/推送日志可查询

### P1（建议有，提升可用性）

- 标题 hash 去重
- 关键词分类
- 用户主题订阅
- 推送失败重试

### P2（可延期）

- LLM 摘要增强
- 重要度评分
- 可视化后台前端
- 个性化推荐

---

## 6. 建议立即补充的文档与产物

为降低返工，建议在开工前补齐以下 6 个“轻量但关键”文档：

1. **系统上下文图**（采集、处理、推送、微信、存储关系）
2. **核心数据表 ER 图**（sources/articles/digests/wx_users/push_logs）
3. **任务时序图**（采集→去重→分类→日报→推送）
4. **错误码与重试策略表**（按模块）
5. **配置清单**（环境变量、密钥、开关）
6. **上线检查清单**（回滚、备份、告警、证书、域名）

---

## 7. 一页式结论

这份任务书已经具备落地基础，核心问题不在“方向”，而在“执行粒度与验收指标”。

如果你按以下三点补齐，成功率会明显提高：

1. 每阶段增加量化验收指标；
2. 将阶段三再拆成可独立验收的子里程碑；
3. 在阶段一提前锁定微信能力边界与替代通道。

这样做可以把“能开发”变成“可按期上线且可持续运维”。
